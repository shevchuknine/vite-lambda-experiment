import React from 'react';

/*
* DESCRIPTION
* context to pass data loaded in lambda function to the application, rendered on the server
* */
const AppContext = React.createContext();
export default AppContext;
